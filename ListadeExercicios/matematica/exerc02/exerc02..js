//2. Escreva um programa que calcule a média aritmética de dois números

const nota1 = 9;
const nota2 = 2;
const media = (nota1 + nota2) / 2;

console.log(`A média das notas ${nota1} e ${nota2} é: ` + media)

// Se estivesse acoplado ao HTML:
// mediaresultado.innerHTML = `A média das notas: ${nota1} e ${nota2} é: ${media} <br>`;