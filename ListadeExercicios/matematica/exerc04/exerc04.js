// Escreva um programa que calcule a média geométrica entre três números informados
// pelo usuário ( a fórmula do cálculo da média geométrica é:
// Math.pow(num1 * num2 * num3, 1/3)

const num1 = Number(10);
const num2 = Number(4);
const num3 = Number(3,4);

const mediageometrica = (Math.pow(num1 * num2 * num3, 1/3));
console.log("A média geométrica das três notas é: ", mediageometrica);